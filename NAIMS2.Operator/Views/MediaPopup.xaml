<Window x:Class="NAIMS2.Common.Views.MediaPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NAIMS2.Common.Views"
        mc:Ignorable="d"
        Title="Popup" Height="400" Width="800"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" BorderBrush="Gray" BorderThickness="1" FontFamily="pretendard" FontSize="25">
    <Window.Resources>
        <Style x:Key="GrayWhiteCircleRadioButtonStyle" TargetType="{x:Type RadioButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RadioButton}">
                        <Grid x:Name="templateRoot" Background="Transparent" SnapsToDevicePixels="True">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="radioButtonBorder" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="1,1,2,1" VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                <Grid x:Name="markGrid" Margin="2">
                                </Grid>
                            </Border>
                            <ContentPresenter x:Name="contentPresenter" Grid.Column="1" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Background" Value="#FFFFFF"/>
                                <!--<Setter TargetName="optionMark" Property="Fill" Value="#FFFFFF"/>-->
                                <!-- 선택 시 하얀색 -->
                            </Trigger>
                            <Trigger Property="HasContent" Value="true">
                                <Setter Property="Padding" Value="4,-1,0,0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Opacity" Value="0.56"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Background="#FF3C3C3C" CornerRadius="10" BorderBrush="Gray" BorderThickness="1">
        <Grid Margin="10,10,8,8">
            <Label Content="USB(:D) 파일 총 00개 (0.000KB)" Margin="50,55,0,274" FontFamily="pretendard" FontSize="14" Background="#FF3C3C3C" Foreground="White"
                   HorizontalContentAlignment="Left" VerticalContentAlignment="Center" />
            <Label Content="저장매체 연동" Margin="0,0,0,353" FontFamily="pretendard" FontSize="14" Background="#FF3C3C3C" Foreground="White"
                   HorizontalContentAlignment="Left" VerticalContentAlignment="Center"/>
            <Border BorderBrush="Gray" BorderThickness="1" Margin="50,126,37,177" RenderTransformOrigin="0.504,-0.525">
                <StackPanel Margin="0,0,9,0" MouseLeftButtonDown="CollectDataStackPanel_Click">
                    <StackPanel.Style>
                        <Style TargetType="{x:Type StackPanel}">
                            <Setter Property="Background" Value="#FF3C3C3C"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsChecked, ElementName=CollectDataRadioButton}" Value="True">
                                    <Setter Property="Background" Value="#000000"/>
                                    <Setter Property="Label.Background" Value="#000000"/>
                                    <Setter Property="Label.Foreground" Value="Gray"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <RadioButton x:Name="CollectDataRadioButton" GroupName="StorageOptions" Content="수집자료 저장" Width="200" FontFamily="pretendard" FontSize="14" Background="#FF3C3C3C" Foreground="White" Height="30" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="5" Style="{StaticResource GrayWhiteCircleRadioButtonStyle}"/>
                    <Label x:Name="lb_codesc" Content="    원음자료,보조정보, 기타 원음자료 관련 파일을 임무 별로 저장" FontFamily="pretendard" FontSize="14" Background="Transparent" Foreground="White" Height="30" VerticalAlignment="Center" Margin="5" MouseLeftButtonDown="CollectDataLabel_Click"/>
                </StackPanel>
            </Border>
            <Border BorderBrush="Gray" BorderThickness="1" Margin="50,233,37,66" RenderTransformOrigin="0.504,-0.525">
                <StackPanel Margin="0,0,9,0" MouseLeftButtonDown="AnalysisResultStackPanel_Click">
                    <StackPanel.Style>
                        <Style TargetType="{x:Type StackPanel}">
                            <Setter Property="Background" Value="#FF3C3C3C"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsChecked, ElementName=AnalysisResultRadioButton}" Value="True">
                                    <Setter Property="Background" Value="#000000"/>
                                    <Setter Property="Label.Background" Value="#000000"/>
                                    <Setter Property="Label.Foreground" Value="Gray"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <RadioButton x:Name="AnalysisResultRadioButton" GroupName="StorageOptions" Content="분석결과 저장" Width="200" FontFamily="pretendard" FontSize="14" Background="#FF3C3C3C" Foreground="White" Height="30" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="5" Style="{StaticResource GrayWhiteCircleRadioButtonStyle}"/>
                    <Label x:Name="lb_andesc" Content="    기준식별DB, 분석결과 보고서를 바로 저장" FontFamily="pretendard" FontSize="14" Background="Transparent" Foreground="White" Height="30" VerticalAlignment="Center" Margin="5" MouseLeftButtonDown="AnalysisResultLabel_Click"/>
                </StackPanel>
            </Border>
            <Button Content="취소" FontFamily="pretendard" FontSize="14" Background="#000000" Foreground="White" Margin="286,338,400,12" Click="Button_Click" Width="90" Height="30" />
            <Button Content="확인" FontFamily="pretendard" FontSize="14" Background="#000000" Foreground="White" Margin="389,338,297,12" Click="Button_Click_1" Width="90" Height="30" />
        </Grid>
    </Border>
</Window>