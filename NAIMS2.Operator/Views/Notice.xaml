<UserControl x:Class="NAIMS2.Common.Views.Notice"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NAIMS2.Common.Views"
             xmlns:viewmodels="clr-namespace:NAIMS2.Operator.ViewModels;assembly=NAIMS2.Operator"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800" Loaded="UserControl_Loaded" Background="Black">
    <UserControl.Resources>
        <Style x:Key="LabelStyle" TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="5,0"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="41*"/>
            <ColumnDefinition Width="39*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Background="Black" Width="800" Grid.ColumnSpan="2">
            <Label Content="공지사항 목록" Background="Transparent" Foreground="White" HorizontalContentAlignment="Left" HorizontalAlignment="Left"
                   VerticalAlignment="Center" VerticalContentAlignment="Center"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Background="Black" Width="800" Height="350" Margin="0,27,0,0" Grid.ColumnSpan="2">
            <DataGrid x:Name="dg_Main" ItemsSource="{Binding NoticeModel}" Width="800" Height="344" Background="LightGray" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center"
                      SelectedItem="{Binding SelectedNotice, Mode=TwoWay}" SelectionChanged="DataGrid_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="No" Binding="{Binding No}" Width="50"/>
                    <DataGridTextColumn Header="제목" Binding="{Binding List}" Width="350"/>
                    <DataGridTextColumn Header="작성자" Binding="{Binding Writer}" Width="100"/>
                    <DataGridTextColumn Header="작성일" Binding="{Binding WriteDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                    <DataGridTextColumn Header="만료일" Binding="{Binding ExpiryDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                    <DataGridTextColumn Header="상태" Binding="{Binding isStatus}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top" Background="Black" Width="800" Height="350" Margin="0,377,0,0" Grid.ColumnSpan="2">
            <!-- 5개의 Label 한 줄에 나열 -->
            <StackPanel Orientation="Horizontal" Background="LightGray" Height="40">
                <Label Content="No:" Style="{StaticResource LabelStyle}" Width="50"/>
                <Label x:Name="lbl_No" Content="{Binding SelectedNotice.No}" Style="{StaticResource LabelStyle}" Width="150"/>
                <Label Content="제목:" Style="{StaticResource LabelStyle}" Width="50"/>
                <Label x:Name="lbl_Title" Content="{Binding SelectedNotice.List}" Style="{StaticResource LabelStyle}" Width="350"/>
                <Label Content="작성자:" Style="{StaticResource LabelStyle}" Width="50"/>
                <Label x:Name="lbl_Writer" Content="{Binding SelectedNotice.Writer}" Style="{StaticResource LabelStyle}" Width="100"/>
                <Label Content="작성일:" Style="{StaticResource LabelStyle}" Width="50"/>
                <Label x:Name="lbl_WriteDate" Content="{Binding SelectedNotice.WriteDate, StringFormat=yyyy-MM-dd}" Style="{StaticResource LabelStyle}" Width="100"/>
                <Label Content="만료일:" Style="{StaticResource LabelStyle}" Width="50"/>
                <Label x:Name="lbl_ExpiryDate" Content="{Binding SelectedNotice.ExpiryDate, StringFormat=yyyy-MM-dd}" Style="{StaticResource LabelStyle}" Width="100"/>
                <Label Content="상태:" Style="{StaticResource LabelStyle}" Width="50"/>
                <Label x:Name="lbl_Status" Content="{Binding SelectedNotice}" Style="{StaticResource LabelStyle}" Width="100"/>
            </StackPanel>
            <!-- ListBox (메모 또는 추가 항목) -->
            <ListBox x:Name="lst_Memo" Height="300" Width="780" Margin="5" Background="LightGray" Foreground="Black"
                     ItemsSource="{Binding SelectedNotice.MemoLines}" 
                     VerticalAlignment="Top" ScrollViewer.VerticalScrollBarVisibility="Auto"/>
        </StackPanel>
        <Button Grid.Column="0" Content="닫기" Background="Transparent" Foreground="White"
        HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Margin="274,732,36,18" Click="Button_Click"/>
        <Button Content="저장" Background="Transparent" Foreground="White"
        HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Margin="33,732,257,18" Click="Button_Click_1"  Grid.Column="1"/>
    </Grid>
</UserControl>