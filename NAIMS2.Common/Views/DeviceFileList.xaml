<Window x:Class="NAIMS2.Common.Views.DeviceFileList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NAIMS2.Common.Views"
        mc:Ignorable="d"
        WindowStyle="None" Height="450" Width="800">
    <Grid Background="#FF3C3C3C">
        <!-- 커스텀 타이틀바 -->
        <Border Background="#FF2D2D2D" Height="30" VerticalAlignment="Top" MouseLeftButtonDown="Border_MouseLeftButtonDown">
            <Grid>
                <TextBlock Text="수집자료 저장" Foreground="White" FontFamily="pretandard" FontSize="14" VerticalAlignment="Center" Margin="10,0,0,0"/>
                <Button x:Name="CloseButton" Content="X" Width="30" Height="30" HorizontalAlignment="Right" Background="Transparent" Foreground="White" BorderThickness="0" Click="CloseButton_Click"/>
            </Grid>
        </Border>

        <!-- 나머지 콘텐츠 -->
        <Grid Margin="10,40,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="340*"/>
                <ColumnDefinition Width="5"/>
                <!-- GridSplitter를 위한 열 -->
                <ColumnDefinition Width="1321*"/>
            </Grid.ColumnDefinitions>

            <Label Content="저장 매체 자료 목록" HorizontalAlignment="Center" Height="29" VerticalAlignment="Center" Width="133" FontFamily="pretandard" FontSize="14" Foreground="White" Grid.Row="0" Grid.Column="0"/>
            <Label x:Name="StatusTextBlock" Content="" HorizontalAlignment="Left" Height="29" VerticalAlignment="Center" Width="Auto" FontFamily="pretandard" FontSize="14" Foreground="White" Grid.Row="0" Grid.Column="2" Margin="10,0,0,0"/>

            <TreeView x:Name="tr_fList" Margin="0,5,0,5" Background="#FF3C3C3C" Grid.Row="1" Grid.Column="0" FontFamily="pretandard" FontSize="14" Foreground="White" SelectedItemChanged="tr_fList_SelectedItemChanged" MouseLeftButtonDown="tr_fList_MouseLeftButtonDown"
                      TreeViewItem.Selected="tr_fList_Selected">
                <TreeView.Resources>
                    <Style TargetType="{x:Type TreeViewItem}">
                        <Setter Property="Foreground" Value="White"/>
                    </Style>
                </TreeView.Resources>
            </TreeView>

            <!-- GridSplitter 추가 -->
            <GridSplitter Grid.Row="1" Grid.Column="1" Width="5" Background="#FF2D2D2D" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

            <DataGrid x:Name="datagrid1" ItemsSource="{Binding FileList}" Grid.Row="1" Grid.Column="2" Background="White" FontFamily="pretandard" FontSize="14" Foreground="White" Margin="0,5,0,5">
                <DataGrid.Resources>
                    <Style TargetType="{x:Type DataGridCell}">
                        <Setter Property="Background" Value="#373737"/>
                        <Setter Property="BorderBrush" Value="#373737"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#373737"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                    <Style TargetType="{x:Type DataGridColumnHeader}">
                        <Setter Property="Background" Value="#373737"/>
                        <Setter Property="Foreground" Value="White"/>
                    </Style>
                    <Style TargetType="{x:Type DataGridRow}">
                        <Setter Property="Background" Value="#373737"/>
                    </Style>
                </DataGrid.Resources>
            </DataGrid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="2" Grid.Column="2" Margin="0,5,0,0">
                <Button x:Name="CancelButton" Content="취소" Width="90" Height="30" Margin="0,0,10,0" FontFamily="pretandard" FontSize="14" Background="#303030" Foreground="White" BorderBrush="#FF3C3C3C" Click="CancelButton_Click" MouseLeftButtonDown="CancelButton_MouseLeftButtonDown"/>
                <Button x:Name="SaveButton" Content="저장" Width="90" Height="30" FontFamily="pretandard" FontSize="14" Background="#303030" Foreground="White" BorderBrush="#FF3C3C3C" Click="SaveButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>